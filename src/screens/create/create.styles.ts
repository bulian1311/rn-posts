import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    padding: 10
  },
  title: {
    textAlign: "center",
    fontSize: 20,
    marginVertical: 10
  },
  textarea: {
    padding: 10,
    marginBottom: 10
  },
  image: {
    width: "100%",
    height: 200,
    marginBottom: 20
  }
});

export default styles;
